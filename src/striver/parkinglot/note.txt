parking lot system design:


STEP 1:


Functional req:


Entry flow:

1. vehicle arrives at gate
2. generate ticket and assign slot based on vehicle type
3. mark slot as occupied
4. return entry result with status

Exit flow:

1. user presents ticket at exit
2. calculate fee based on pricing rules (like min of hourly, flat etc. )
3. process payment through payment gateway
4. generate receipt and mark slot unoccupied
5. return exit result with status

Admin flow / configurations :

1. CRUD on floors, slots
2. define pricing rules based on vehicle type
3. update pricing fee ( flat, hourly )
4. view current parking status

------------------------------------------------------------------------------------------------

Non functional req: ( HLD focused )


Scalability: must support multiple parking lot and thousands of slots

Consistency: strong consistency for slot allocation and release

Availability: HA for entry/exit gate even during payment failure

Latency: low latency ( < 500ms ) for ticket generation and payment processing

Extensibility: easily add new vehicle types, pricing strategies, gateways

Security: RBAC for admin actions

------------------------------------------------------------------------------------------------


Edge cases:

1. payment failure during exit - retry and hold slot

2. ticket lost - allow admin override

3. clock skew ( IMPT ) - system time validation for all servers worldwide

4. slot state mismatch  - periodic reconciliation ( slot showing occupied, but still empty )

------------------------------------------------------------------------------------------------


STEP 2: Identify core entities


1. Vehicle

id: UUID (pk)
licensePlate: String
vehicleType: Enum ( CAR, BIKE, TRUCK, EV )

2. ParkingSlot

id: UUID (pk)
slotType: Enum
isOccupied: boolean
floorNumber: int

3. Floor

id: UUID (pk)
number: int
slots: List<ParkingSlot>

4. Ticket ( is the relation b/w slot and vehicle based on vehicle type )

id: UUID (pk)
vehicleId: (FK)
slotId: (FK)
entryTime: Timestamp
isActive: boolean

5. Receipt ( after payment, ticket is used to generate receipt )

id: UUID (pk)
ticketId: (FK)
exitTime: Timestamp
totalFee: (based on pricing rule)
paymentStatus: SUCCESS, FAILED, PENDING

6. PricingRule

id: UUID (pk)
vehicleType: enum
ratePerHour: double
flatRate: double
ruleType: enum (flat, hourly)

7. Payment

id: UUID (pk)
ticketId: (FK) ( correction in image required, not doing )
amount: double
gateway: enum
status: enum

DTO

1. EntryResult

success: boolean
ticket: Ticket
message: String

2. ExitResult

success: boolean
receipt: Receipt
message: String
------------------------------------------------------------------------------------------------

at this point, write code for all the domain entities, initialize their fields, and their default constructor, and some other methods in domain classes, if you need, or you can write them on the go
------------------------------------------------------------------------------------------------

Step 3: Discuss interaction flow ( for converting business into design patterns )

==> showing the thinking to convert the flow into class:


1. Entry flow:

-> driver enters, gets a slot (based on vehicle type), gets a ticket

=> here, ( gets a slot ) means that a class that manages the slot ( using SRP ), similarly for (gets a ticket) means a class that manages the ticket, using SRP



2. Exit flow:

-> driver exit, shows the ticket, price computed ( based on min of pricing ), pay's the amount ( with retries if it fails ), gets the receipt, slot released, ticket deactivated to avoid re-entry

=> (shows the ticket) means verifying ticket is valid or not etc. , ( using SRP, a class that manages Ticket )
(price computed ) means that i need something pricing kind of thing (Payment) to compute the price
(paying with retry) means a manager that takes care with retryable payments
(gives the receipt ) means 1 more class that gives the receipt
(slot released ) can be done the slot manager
(ticket deactivated ) can be done by the ticket class


3. Admin flow:

-> admin add slots, floors, update pricing
=> can all be done as 1 admin class only, since behavior attached to admin


Don't code them yet. first discuss them in step 4
------------------------------------------------------------------------------------------------


Step 4: Discuss class structures and class relationships


NOTE: don't add classes to just 1 package, keep them separate based on architectures

ARCH LAYERS:

client/UI -> Controller layer ( entrypoint: HTTP/API) -> Service layer ( business logic ) -> Repository layer -> domain layer


Controllers: (entrypoint):

NOTE: if you have less number of arguments, avoid DTOs in interview.

1. EntryController
- method: EntryResult enterVehicle(String licensePlate, VehicleType type) // having separate args to demonstrate vehicle creation

2. ExitController
- ExitResult(UUID ticketId)

3. AdminController
- multiple methods

NOTE: business logic is the easiest part to do in LLD interview, design is the most important part

at this point, just create the controller, default constructor and their dependencies.




Service layers (very simple based on the flow), write method names.

1. TicketService
- Ticket generateTicket(Vehicle, ParkingSlot)
- Ticket getTicket(ticketId)

2. SlotService
- ParkingSlot allocateSlot(VehicleType)
- void releaseSlot(slotId)

3. PricingService ( for price computation )
- double calculateFee(ticketId) // based on pricing fee, return amount to pay

4. PaymentService
- boolean processPayment(ticketId, amount)

5. ReceiptService
- Receipt generateReceipt(ticket, fee, paymentSuccess)

6. AdminService
- void addFloor(int floorNumber)
- void addSlot(int floorNumber, Slot slot)
- void updatePricing(VehicleType, ratePerHour, fixedRate)
- void updateFlatPricing, updateHourlyPricing





Repository Layer:

NOTE: typically for this, try to connect with the service layer, NOT with the domain layer

1. TicketRepository
- void save(Ticket)
- Ticket findById(ticketId)
- List<Ticket> findActiveTickets()
- void deactivateTicket(ticketId)

2. SlotRepository
- void save(Slot)
- ParkingSlot findById(slotId)
- ParkingSlot findAvailableSlot(VehicleType)

3. FloorRepository ( use only for floor )
- void save
- Floor findByFloorNumber(int number)

4. PricingRepository
- void save
- PricingRule findByVehicleType(Veh.Type)

5. PaymentRepository
- void save(Payment)
- Payment findByTicketId(ticketId)


Since we have 3 party integration ( payment ), using adapter pattern


Interfaces:

1. PaymentGatewayAdapter
- boolean pay(UUID ticketId, double amount)

impls: RayzorpayAdaptor, StripeAdapter

------------------------------------------------------------------------------------------------


At this point, just write skeleton for service layer, just the methods and init dependencies, them similarly for repository. just write method defined above, their args etc. not method bodies.


replicate DB tables with Map (concurrent )

now code for adapters


at this point, all the skeleton code should be done, all methods in place, all dependencies initialized etc. just method body required. ( most likely for service layers only). repository, adapter layer methods should also be completed.


Use constructor injection
------------------------------------------------------------------------------------------------


Step 5: Core use cases and methods:

-> here, trying to get the interaction flow in terms of business logic


1. Entry use case

enterVehicle() -> SlotService.allocateSlot() -> TicketService.generateTicket() -> TicketRepository.save() -> return EntryResult


2. Exit use case

exitVehicle() -> TicketRepository.getTicket() -> PricingService.calculateFee() -> PaymentService.processPayment() -> PaymentGatewayAdapter.pay() -> SlotService.releaseSlot() -> ReceiptService.generateReceipt() -> return ExitResult


3. Admin use case

addFloor() ->
addSlot() ->
updatePricing -> PricingRepository.save()


NOTE: based on these flows, you will determine that which dependencies are required by the controller, and then you can constructor init them.


















































